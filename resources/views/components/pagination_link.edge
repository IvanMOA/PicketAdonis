<a href="{{ disabled ? '#' : `/admin/events?page=${pagination.currentPage + 1}`}}" class="{{ disabled && 'pointer-events-none cursor-not-allowed opacity-50' }} bg-white border-gray-300 text-gray-500 hover:bg-gray-50 relative inline-flex items-center px-4 py-2 border text-sm font-medium" {{ $props.serialize() }}>
   {{{ await $slots.main() }}}
</a>
