@layout('layouts/main')
@set('title', 'Login')
@section('body')
<section>
  <div class="flex items-center justify-center min-h-screen bg-gray-100">
      <div class="px-8 py-6 mx-4 mt-4 text-left bg-white shadow-lg md:w-1/3 lg:w-1/3 sm:w-1/3">
          <h3 class="text-2xl font-bold text-center">Administrator login</h3>
          <form v-scope="{ isSubmitting: false }" @submit="isSubmitting = true"  action="/admin/login" method="POST">
              <div class="mt-4">
                  @!component('components/input', { name: "email", label: 'Email', type: 'email', placeholder: 'youremail@gmail.com', errors: flashMessages.get("errors.email") })
                  @!component('components/input', { name: "password", label: 'Password', type: 'password', placeholder: 'Password', errors: flashMessages.get("errors.password")})
                  <div class="flex">
                    @!component('components/button', { text: "Login", disabled: "isSubmitting" })
                  </div>
                  <div class="mt-6 text-grey-dark">
                      Don't have an account yet?
                      <a class="text-blue-600 hover:underline" href="/register">
                          Create one
                      </a>
                  </div>
              </div>
          </form>
      </div>
  </div>
</section>
@end
